<Window x:Class="BD.Window6"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BD"
        mc:Ignorable="d"
        Title="Window6" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="DGridKatalog" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название микрофона" Binding="{Binding Name}" Width="*"></DataGridTextColumn>
                <DataGridTextColumn Header="Минимальная частота" Binding="{Binding Min_frequency}" Width="*"></DataGridTextColumn>
                <DataGridTextColumn Header="Максимальная частота" Binding="{Binding Max_frequency}" Width="*"></DataGridTextColumn>
                <DataGridTextColumn Header="Производитель" Binding="{Binding proizvoditel.Name}" Width="*"></DataGridTextColumn>
                <DataGridTextColumn Header="Тип микрофона" Binding="{Binding microfon_type.Name}" Width="*"></DataGridTextColumn>
                <DataGridTextColumn Header="Гарантия" Binding="{Binding Garantiya.Name}" Width="*"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Назад" HorizontalAlignment="Right" Name="Btnback" Click="BtnBack_Click" Height="45" VerticalAlignment="Bottom" RenderTransformOrigin="-0.553,-0.345" Margin="0,0,10,10" Width="95"/>
        <Button Content="Редактировать" HorizontalAlignment="Left" Name="BtnRed" Click="BtnRed_Click" Height="45" Margin="10,364,0,0" VerticalAlignment="Top" Width="116"/>
        <Button Content="Добавить" HorizontalAlignment="Left" Name="BtnDob" Click="BtnDob_Click" Height="45" Margin="170,364,0,0" VerticalAlignment="Top" Width="75"/>
        <Button Content="Удалить" HorizontalAlignment="Left" Name="BtnClear" Click="BtnClear_Click" Height="45" Margin="532,364,0,0" VerticalAlignment="Top" Width="74" RenderTransformOrigin="0.14,0.611"/>
    </Grid>
</Window>
